# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# pytest
.pytest_cache/
.coverage

# colcon (when built in-tree)
install/
log/

# Detector weights are baked into the image at build time, never committed.
*.pt
*.onnx
*.engine
